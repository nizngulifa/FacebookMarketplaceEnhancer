# No environment variables are required for the extension today.
# When you add optional API keys or feature flags, document them here only —
# never commit real secrets.
#
# Database and MCP URLs belong at the repo root (.env), not in the extension.
